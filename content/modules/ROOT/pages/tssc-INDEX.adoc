= Lightwell and Trusted Software Supply Chain Labs Overview
:toc:
:toclevels: 2
:icons: font

xref:index.adoc[← Back to HOME]

== Overview

These labs walk a trusted software supply chain on this OpenShift Security Roadshow cluster. You verify a signed Hummingbird runtime, consume Lightwell application libraries through an enterprise proxy, bind those pins in source, build without the public internet, sign and attest the image, promote only that digest, then keep proof in Trusted Profile Analyzer and Advanced Cluster Security.

This is a product deep-dive, not a 101, 201, or 301 skill level. Work the labs in order. Hands-on work starts at xref:tssc-01-hummingbird-verify.adoc[1.1 Verify Hummingbird].

The lab application is Java (Maven). Where the same control applies to Python (`pip`) or Artifactory, that page says so in one sentence.

== Why it matters

Scanners tell you what is wrong. They do not give you a signed runtime, a patched library you can actually consume, or a path that keeps that proof through build, sign, and promote.

Public indexes and mutable tags are convenient. They are not a trusted input. A production container still has to answer what went into the image, who built and signed it, and whether known vulnerabilities were remediated on the versions you already run.

link:{lightwell-product-url}[Lightwell^] covers *third-party application dependencies* (libraries, frameworks, build tools, and transitive deps). link:{docs-hardened-images-url}[Red Hat Hardened Images^] (Hummingbird) are the *runtime* foundation. OpenShift, RHEL, and RHACS still cover the platform. Lightwell does not replace those products.

== What does it solve

* CVE findings on libraries you cannot jump to “latest” because of certification, compatibility, or a release window
* Application builds that pull from the public internet instead of a trusted enterprise repository
* Runtime bases that were never signature-checked before they entered the cluster
* Images that reach production without a digest pin, a signature, or an SBOM
* Builds that succeed only because Maven Central or a public registry was reachable
* No system of record for what shipped, what was fixed, and what ACS should treat as remediated

== How Lightwell and TSSC fit together

A *trusted software supply chain* (TSSC) is the set of controls that keep those answers attached to every production container.

Red Hat TSSC on OpenShift uses

* **Signed and attested artifacts** from link:{docs-rhtas-url}[Red Hat Trusted Artifact Signer^]
* **SBOMs and advisories** in link:{docs-rhtpa-url}[Red Hat Trusted Profile Analyzer^]
* **Runtime and pipeline policy** with link:{docs-rhacs-url}[Red Hat Advanced Cluster Security^] and Conforma / Enterprise Contract
* **Promotion** with link:{docs-gitops-url}[OpenShift GitOps^]
* **A golden path for developers** in link:{docs-rhdh-url}[Red Hat Developer Hub^]

This same stack powers link:{docs-rhtap-tech-url}[Red Hat Trusted Application Pipeline^].

TSSC does not start at the pipeline. It starts with inputs you can name. A signed runtime base. Application libraries from a trusted repository. Source that binds those pins.

Lightwell is an annual subscription with two models. This workshop uses the *Lightwell Network* path (public, post-disclosure remediations). *Lightwell Clearinghouse Premier* adds member-specific package versions and disclosure handling. A Premier remediation still uses the same proxy, pin, hermetic build, and TPA or ACS evidence.

[cols="1,2,2",options="header"]
|===
|Model |What you get |Challenge it addresses

|*Lightwell Network* (available)
|Remediations for eligible open source vulnerabilities. *Validated* repositories stay at upstream-parity (signed, with SBOM). *Remediated* repositories back-port security-only fixes with a `.rhlw-*` version suffix. You consume them through the build and artifact-manager workflows you already have.
|You need a patched library on the version you already run, not a major upgrade. Tracks 2, 3, and 7. Docs are link:{docs-lwn-choose-repo-url}[Choose the right repository^] and link:{docs-lwn-java-url}[Configure your Java build tool^]. Console is {lightwell-console-url}.

|*Lightwell Clearinghouse Premier* (limited availability)
|All Network benefits, plus remediation for *member-specific* package versions, novel vulnerability verification, secured patch embargoes, anonymized access to other member requests, and TAM services.
|Your pinned version is not in the public catalog, or the fix must stay in a closed set until a public disclosure date. Same TSSC controls as Network. This cluster does not include a live Premier membership.
|===

An *embargo* is a coordinated-disclosure window. The vulnerability and its patch stay in a closed member set until a public date. Clearinghouse Premier can deliver that member pin during the window. Lightwell Network’s documented lifecycle ends at public *Disclosure* (VEX and OSV to the broader ecosystem). See link:{docs-lwn-lifecycle-url}[Patch delivery lifecycle^].

More background is in xref:appendix-lightwell-concepts.adoc[Lightwell concepts].

// VISUAL: https://github.com/NA-FSI-Services/lightwell-tssc-workshop/issues/109
image::tssc-lwn-clearinghouse-overview.png[TSSC controls wrapping Lightwell Network catalog consumption and Clearinghouse Premier member remediations,width=700,link=self,window=blank]

// VISUAL: https://github.com/NA-FSI-Services/lightwell-tssc-workshop/issues/110
image::lwn-vs-clearinghouse-premier.png[Lightwell Network catalog versus Clearinghouse Premier member-specific remediations, both landing in the same TSSC proxy, pin, and system of record,width=700,link=self,window=blank]

== Recommended sequence

Complete the tracks in order. Later labs assume the earlier pins, signatures, and GitOps commits are already in place.

On each numbered page, *Your change* is the edit you make. *Check* is the commands that should succeed afterward. Do both before you continue.

// VISUAL: https://github.com/NA-FSI-Services/lightwell-tssc-workshop/issues/48
image::seven-track-tssc-flow.png[Seven-track TSSC flow from Hummingbird through continuous compliance,width=700,link=self,window=blank]

. xref:tssc-01-hummingbird-verify.adoc[1.1 Verify Hummingbird]
. xref:tssc-02-hummingbird-mirror.adoc[1.2 Mirror into the trust boundary]
. xref:tssc-03-enterprise-proxy.adoc[2.1 Enterprise proxy]
. xref:tssc-04-remediated-pin.adoc[2.2 Remediated pin]
. xref:tssc-05-golden-path-source.adoc[3.1 Golden-path source]
. xref:tssc-06-bind-base-deps.adoc[3.2 Bind base and deps]
. xref:tssc-07-live-renovate.adoc[3.3 Live Renovate]
. xref:tssc-08-forbid-list.adoc[4.1 Source forbid-list]
. xref:tssc-09-prefetch.adoc[4.2 Prefetch (Hermeto mapping)]
. xref:tssc-10-build-networkpolicy.adoc[4.3 Build NetworkPolicy]
. xref:tssc-11-build-sbom.adoc[4.4 Build and SBOM]
. xref:tssc-12-sign-keyless.adoc[5.1 Sign (keyless)]
. xref:tssc-13-attest-conforma.adoc[5.2 Attest + Conforma]
. xref:tssc-14-disconnected-verify.adoc[5.3 Disconnected verify]
. xref:tssc-15-gitops-admission.adoc[6.1 GitOps + admission]
. xref:tssc-16-prod-gitops.adoc[6.2 Promote to prod repo]
. xref:tssc-17-sbom-sor.adoc[7.1 SBOM system of record]
. xref:tssc-18-vex-acs.adoc[7.2 VEX blast radius + ACS]

== Labs index

// VISUAL: https://github.com/NA-FSI-Services/lightwell-tssc-workshop/issues/111
image::tssc-artifact-trust-chain.png[Trust chain from Hummingbird digest and Lightwell GAV through signed app image to TPA VEX,width=700,link=self,window=blank]

[cols="1,2,3",options="header"]
|===
|Lab |Title |Challenge it addresses

|xref:tssc-01-hummingbird-verify.adoc[1.1]
|Verify Hummingbird
|Runtime bases that were never signature-checked

|xref:tssc-02-hummingbird-mirror.adoc[1.2]
|Mirror into the trust boundary
|Catalog images that never entered your registry

|xref:tssc-03-enterprise-proxy.adoc[2.1]
|Enterprise proxy
|Builds that pull libraries from Maven Central

|xref:tssc-04-remediated-pin.adoc[2.2]
|Remediated pin
|“Upgrade to latest” when you must stay on a certified version

|xref:tssc-05-golden-path-source.adoc[3.1]
|Golden-path source
|Application source that is not yours to pin and promote

|xref:tssc-06-bind-base-deps.adoc[3.2]
|Bind base and deps
|Dockerfiles and Maven pins that still float

|xref:tssc-07-live-renovate.adoc[3.3]
|Live Renovate
|Manual pin edits that drift from the remediation catalog

|xref:tssc-08-forbid-list.adoc[4.1]
|Source forbid-list
|Public `FROM`, Maven Central, or `curl` still in active files

|xref:tssc-09-prefetch.adoc[4.2]
|Prefetch (Hermeto mapping)
|Dependencies fetched from the internet at build time

|xref:tssc-10-build-networkpolicy.adoc[4.3]
|Build NetworkPolicy
|Build pods that can egress anywhere

|xref:tssc-11-build-sbom.adoc[4.4]
|Build and SBOM
|Production images with no SBOM

|xref:tssc-12-sign-keyless.adoc[5.1]
|Sign (keyless)
|Application images with no signature

|xref:tssc-13-attest-conforma.adoc[5.2]
|Attest + Conforma
|Policy that never fails, so unsigned work still ships

|xref:tssc-14-disconnected-verify.adoc[5.3]
|Disconnected verify
|No way to verify a signature when public key servers are out of reach

|xref:tssc-15-gitops-admission.adoc[6.1]
|GitOps + admission
|Unsigned images that still deploy to the cluster

|xref:tssc-16-prod-gitops.adoc[6.2]
|Promote to prod repo
|Production that is not a separate, digest-pinned promote

|xref:tssc-17-sbom-sor.adoc[7.1]
|SBOM system of record
|No durable record of what the promoted digest contains

|xref:tssc-18-vex-acs.adoc[7.2]
|VEX blast radius + ACS
|Scanner noise with no GAV-bound VEX, and ACS left out of the story
|===

[[what-this-workshop-does-not-include]]
== What this workshop does not include

You walk *one* Java (Maven) supply chain on this OpenShift environment. The items below are out of scope on purpose.

[cols="1,2",options="header"]
|===
|Not in this workshop
|What you do instead

|Hosted Konflux
|Builds run as *OpenShift Pipelines* (the Task and BuildConfig names on the cluster). Equivalent Konflux names are in xref:appendix-konflux-mapping.adoc[Konflux mapping]. Do not install Konflux here.

|A physical air-gap or data diode
|This environment stays on the network. xref:tssc-14-disconnected-verify.adoc[5.3] is the key-based sign and verify procedure. xref:appendix-disconnected-playbook.adoc[Disconnected transfer] is a simulated file drop, not USB or a diode.

|Building Project Hummingbird images
|You *verify and consume* published Red Hat Hardened Images. You do not rebuild the factory that produces them.

|FIPS certification in the lab
|FIPS is a *product claim* on the image you consume. This workshop does not rebuild an image to produce FIPS evidence.

|JFrog Artifactory, or a Python (FastAPI) application
|The lab application is Java. *Nexus* is the enterprise proxy you configure. Short callouts cover `pip` and Artifactory patterns. You do not install Artifactory or scaffold a second app.

|A live Clearinghouse Premier membership, member-specific GAV, or embargo window
|You practice the *post-disclosure* Network pin and the TSSC controls Premier would reuse (private proxy, exact pin, system of record). You do not request a member-only package on this cluster. Concept is in xref:appendix-lightwell-concepts.adoc[Lightwell concepts].

|A production virtual machine
|You promote a *signed container digest* through GitOps (xref:tssc-15-gitops-admission.adoc[6.1], xref:tssc-16-prod-gitops.adoc[6.2]). The same digest can later run on bootc or Satellite. There is no VM to boot here.
|===

== How checks work

Every lab ends with a *Check*. It is a Job in `lw-poc-validate`. You set a short report token, run the Job, and read the logs.

* `CHECK PASSED` means you can continue.
* `CHECK FAILED` names what to fix. There is no Solve. Delete the Job and run it again after you fix it.

Try the 1.1 Check here so you can see the flow. It will fail until you finish *Your change* on xref:tssc-01-hummingbird-verify.adoc[1.1 Verify Hummingbird]. A fail that names an object means the Check is working.

:report-name: report-01-hummingbird-verify
:report-key: consume_published
:report-token: consume-published
include::partial$validate-job-report.adoc[]

:validate-job: validate-01-hummingbird-verify
:validate-job-template: job-01.yaml
include::partial$validate-job-rerun.adoc[]

The same commands appear on every *Check*, with that lab’s Job name.

== Before you start

* This guide runs in the RHDP *Showroom* (lab nav plus embedded terminal).
* `oc`, `cosign`, `oc-mirror`, and `ec` are already on `PATH`. Do *not* curl `github.com` for CLIs.
* Console URLs and passwords for Gitea, Keycloak, RHTPA, and Developer Hub are on xref:index.adoc[Welcome]. CLI steps still discover hosts from `demo-userinfo-*` ConfigMaps. Do not invent Route hosts. Do not use GitHub as lab git. Gitea is in-cluster.
* Checks are Validate Jobs in `lw-poc-validate`. The 1.1 example above is how you run one.
* Never commit credentials (`LW_USERNAME` / `LW_PASSWORD`, `student_password`, `cosign.key`).

== Related documentation

* link:{lightwell-product-url}[Lightwell^] (Network and Clearinghouse Premier)
* link:{docs-lwn-lifecycle-url}[Patch delivery lifecycle for Lightwell Network^]
* link:{docs-lwn-choose-repo-url}[Choose Validated or Remediated^]
* link:{docs-lwn-java-url}[Configure your Java build tool for Lightwell Network^]
* link:{docs-hardened-images-url}[Verify Red Hat Hardened Images^]
* link:{docs-oc-mirror-url}[oc-mirror plugin v2^]
* link:{docs-rhtap-tech-url}[Technologies behind Trusted Application Pipeline^] (RHTAS, RHTPA, RHDH)
* link:{docs-rhtas-url}[Trusted Artifact Signer deployment guide^] (includes Conforma / `ec`)
* link:{docs-rhtpa-url}[Trusted Profile Analyzer overview^]
* link:{docs-rhacs-url}[Advanced Cluster Security. Evaluate security risks^]
* link:{docs-gitops-url}[OpenShift GitOps^]
* link:{docs-pipelines-url}[OpenShift Pipelines^]
* link:{docs-rhdh-url}[Red Hat Developer Hub^]

== Start here

Open xref:tssc-01-hummingbird-verify.adoc[1.1 Verify Hummingbird] when you are ready to work.
