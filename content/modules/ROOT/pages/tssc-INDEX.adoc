= Lightwell Software Supply Chain Security Workshop
:toc:
:toclevels: 2

xref:index.adoc[← Back to HOME]

This track is the Lightwell TSSC workshop on the OpenShift Security Roadshow cluster. You verify a Hummingbird base, consume Lightwell dependencies, own the application source, build hermetically, sign and attest, promote a production container, then prove continuous compliance.

The lab application is Java (Maven). Where the same control applies to Python (`pip`) or Artifactory, the page says so in one sentence.

This page is a map, not an exercise. There is nothing to pass here. Hands-on work starts at xref:tssc-01-hummingbird-verify.adoc[1.1 Verify Hummingbird]: you discover the published Hummingbird pin and run `cosign verify` against it.

== What is a trusted software supply chain

A *trusted software supply chain* (TSSC) is the set of controls that let you prove, for every production container, *what* went into the image, *who* built and signed it, and *whether* known vulnerabilities are remediated without pretending the public internet is a trusted input.

Red Hat TSSC provides that proof on OpenShift through:

* **Signed and attested artifacts** - link:{docs-rhtas-url}[Red Hat Trusted Artifact Signer^]
* **System of record for SBOMs and advisories** - link:{docs-rhtpa-url}[Red Hat Trusted Profile Analyzer^]
* **Runtime and pipeline policy** - link:{docs-rhacs-url}[Red Hat Advanced Cluster Security^], Conforma / Enterprise Contract
* **GitOps promotion** - link:{docs-gitops-url}[OpenShift GitOps^]
* **Golden path for developers** - link:{docs-rhdh-url}[Red Hat Developer Hub^]

This same technology stack powers link:{docs-rhtap-tech-url}[Red Hat Trusted Application Pipeline^].

TSSC does not start at the pipeline. It starts with *inputs you can name*: a signed runtime base, application libraries that come from a trusted repository, and source that binds those pins. That is where Lightwell and Hardened Images sit.

// VISUAL: https://github.com/NA-FSI-Services/lightwell-tssc-workshop/issues/109
image::tssc-lwn-clearinghouse-overview.png[TSSC controls wrapping Lightwell Network catalog consumption and Clearinghouse Premier member remediations,width=700,link=self,window=blank]

== Lightwell Network and Lightwell Clearinghouse Premier

link:{lightwell-product-url}[Lightwell^] is the Red Hat and IBM initiative that extends enterprise open source maintenance to *third-party application dependencies* (libraries, frameworks, build tools, transitive deps). Scanners find CVEs; teams often cannot upgrade because of certification, compatibility, or release windows. Lightwell supplies surgical security remediations on the versions you already run.

Lightwell is an annual subscription with two models.

[cols="1,2,2",options="header"]
|===
|Model |What it is |How it shows up in *this* TSSC lab

|*Lightwell Network* (available)
|Consolidated access to Lightwell remediations for eligible open source vulnerabilities. Repositories are *Validated* (upstream-parity, signed, SBOM) and *Remediated* (security-only backports with a `.rhlw-*` version suffix). Consume through existing build and artifact-manager workflows.
|Tracks 2, 3, and 7: enterprise Nexus proxy, exact `.rhlw-*` pin from an OSV `fixed` event, GAV-bound VEX/SBOM in TPA. Docs: link:{docs-lwn-choose-repo-url}[Choose the right repository^], link:{docs-lwn-java-url}[Configure your Java build tool^]. Console: {lightwell-console-url}.

|*Lightwell Clearinghouse Premier* (limited availability)
|All Network benefits, plus vulnerability reporting and remediation for *member-specific* package versions, novel vulnerability verification and *disclosure handling* (secured patch embargoes), anonymized access to other member requests, and TAM services. Aimed at pre-selected critical infrastructure, with a broader release planned.
|Not a separate cluster. A Premier remediation is still a pinned GAV (or `+rhlw.*` wheel) that must land in the *same* enterprise proxy, the *same* source pin, the *same* hermetic build, and the *same* TPA/ACS evidence. Tracks 2, 3, and 7 are the controls you would configure for that member version, including during an embargo (private proxy, pin from the GAV, delay public OSV/VEX).
|===

An *embargo* is a coordinated-disclosure window: the vulnerability and its patch stay in a closed member set until a public date. Clearinghouse Premier can deliver that member pin *during* the window. Lightwell Network’s documented lifecycle ends at public *Disclosure* (VEX and OSV to the broader ecosystem; link:{docs-lwn-lifecycle-url}[Patch delivery lifecycle^]). This workshop scores the *post-disclosure* Network path. The TSSC controls do not change; the timing of public OSV/VEX does. There is no embargo exercise on this claim. More: xref:appendix-lightwell-concepts.adoc[Lightwell concepts].

// VISUAL: https://github.com/NA-FSI-Services/lightwell-tssc-workshop/issues/110
image::lwn-vs-clearinghouse-premier.png[Lightwell Network catalog versus Clearinghouse Premier member-specific remediations, both landing in the same TSSC proxy, pin, and system of record,width=700,link=self,window=blank]

Do *not* treat Lightwell as a replacement for RHEL, OpenShift, or ACS. Those products cover OS and platform. Lightwell covers application-layer dependencies. Do *not* treat it as a replacement for Hummingbird / link:{docs-hardened-images-url}[Red Hat Hardened Images^] either: Hardened Images are the *runtime foundation*; Lightwell is the *library* layer. Commercial SKUs, pricing, and associate-only sales guidance stay off this page (xref:appendix-lightwell-concepts.adoc[Lightwell concepts]).

== How this workshop maps to TSSC

Observe these TSSC objectives as you work. Each gated module exists so you can *see* the control, not only complete a ConfigMap.

// VISUAL: https://github.com/NA-FSI-Services/lightwell-tssc-workshop/issues/111
image::tssc-artifact-trust-chain.png[Trust chain from Hummingbird digest and Lightwell GAV through signed app image to TPA VEX,width=700,link=self,window=blank]

[cols="1,2,2",options="header"]
|===
|TSSC objective |Where you observe it |Lightwell Network / Clearinghouse Premier

|Trusted *runtime* input (signed minimal base)
|xref:tssc-01-hummingbird-verify.adoc[1.1] published `cosign verify`; xref:tssc-02-hummingbird-mirror.adoc[1.2] dest digest after oc-mirror
|Complementary. Hummingbird is not a Lightwell GAV. Premier does not replace Hardened Images.

|Trusted *library* input (enterprise proxy, not the public index)
|xref:tssc-03-enterprise-proxy.adoc[2.1] Maven `settings.xml` against Nexus Validated/Remediated
|Network: this is how customers consume `{java-validated-url}` and `{java-remediated-url}`. Premier member versions (including embargoed GAVs) use the *same* remotes and must not fall through to Maven Central.

|Exact-version remediation (not “upgrade to latest”)
|xref:tssc-04-remediated-pin.adoc[2.2] OSV `fixed` → `.rhlw-*` default pin
|This Check is *post-disclosure* Network OSV. Under a Premier embargo you may already have the `.rhlw-*` GAV in the private proxy *before* public OSV exists; still pin that exact version.

|Source of truth binds those pins
|xref:tssc-05-golden-path-source.adoc[3.1] learner-owned Gitea; xref:tssc-06-bind-base-deps.adoc[3.2] dest `FROM` + default pin; xref:tssc-07-live-renovate.adoc[3.3] bot PR
|Renovate (MintMaker in a client engagement) is how new Network or Premier pins arrive without a hand-edit. During embargo, merge the pin; do not paste the advisory into a public PR description.

|Hermetic production of the image (source, prefetch, network)
|xref:tssc-08-forbid-list.adoc[4.1]–xref:tssc-11-build-sbom.adoc[4.4]
|Lightwell GAVs must resolve from the internal proxy after egress is denied. Premier does not add a public fallback.

|Integrity of the *app* artifact (sign, policy, disconnected verify)
|xref:tssc-12-sign-keyless.adoc[5.1]–xref:tssc-14-disconnected-verify.adoc[5.3]
|You sign the application image. Lightwell already signed the library. Distinct layers.

|Only the signed digest may run, and prod is a separate promote
|xref:tssc-15-gitops-admission.adoc[6.1]–xref:tssc-16-prod-gitops.adoc[6.2]
|Admission does not care which Lightwell model produced the GAV. It cares that the *image* is signed.

|Continuous compliance (SoR + blast radius)
|xref:tssc-17-sbom-sor.adoc[7.1]–xref:tssc-18-vex-acs.adoc[7.2]
|Network: GAV-bound VEX/OSV for the pin (public, post-disclosure). Premier: member advisories ingest into the same TPA; during embargo that metadata stays off public scanner feeds.
|===

== Seven tracks

// VISUAL: https://github.com/NA-FSI-Services/lightwell-tssc-workshop/issues/48
image::seven-track-tssc-flow.png[Seven-track TSSC flow — Hummingbird through continuous compliance,width=700,link=self,window=blank]

Work the tracks in order. On each numbered page, *Your change* is the edit you make; *Check* is the commands that should succeed afterward. Do both before you continue. Later pages assume the earlier work landed.

.Track 1 — Hummingbird
* xref:tssc-01-hummingbird-verify.adoc[1.1 Verify Hummingbird] — published digest and Hardened Images key
* xref:tssc-02-hummingbird-mirror.adoc[1.2 Mirror into the trust boundary] — learner-run oc-mirror into dest

.Track 2 — Lightwell dependencies
* xref:tssc-03-enterprise-proxy.adoc[2.1 Enterprise proxy] — author `settings.xml` against in-cluster Nexus
* xref:tssc-04-remediated-pin.adoc[2.2 Remediated pin] — exact `.rhlw-*` from a Lightwell OSV fixed event

.Track 3 — Source
* xref:tssc-05-golden-path-source.adoc[3.1 Golden-path source] — learner-owned Gitea remotes
* xref:tssc-06-bind-base-deps.adoc[3.2 Bind base and deps] — dest Hummingbird `FROM` plus Lightwell pins
* xref:tssc-07-live-renovate.adoc[3.3 Live Renovate] — merge a real `renovate-bot` PR, not a hand-edit

.Track 4 — Hermetic build
* xref:tssc-08-forbid-list.adoc[4.1 Source forbid-list] — no public `FROM` / Central / `curl` in active files
* xref:tssc-09-prefetch.adoc[4.2 Prefetch (Hermeto mapping)] — Task `prefetch-dependencies`
* xref:tssc-10-build-networkpolicy.adoc[4.3 Build NetworkPolicy] — `build-egress` on `lw-poc-build`
* xref:tssc-11-build-sbom.adoc[4.4 Build and SBOM] — OpenShift BuildConfig image plus SBOM

.Track 5 — Signed and attested image
* xref:tssc-12-sign-keyless.adoc[5.1 Sign (keyless)] — RHTAS Fulcio / Rekor / TUF on the *app* digest
* xref:tssc-13-attest-conforma.adoc[5.2 Attest + Conforma] — tighten policy; fail then pass
* xref:tssc-14-disconnected-verify.adoc[5.3 Disconnected verify] — second signature with `--key` (cluster stays networked)

.Track 6 — Production container
* xref:tssc-15-gitops-admission.adoc[6.1 GitOps + admission] — stage promote; unsigned deny
* xref:tssc-16-prod-gitops.adoc[6.2 Promote to prod repo] — commit the signed digest to the prod remote

.Track 7 — Continuous compliance
* xref:tssc-17-sbom-sor.adoc[7.1 SBOM system of record] — ingest the promoted digest into TPA
* xref:tssc-18-vex-acs.adoc[7.2 VEX blast radius + ACS] — Lightwell GAV-bound VEX; ACS not skipped

[[what-this-workshop-does-not-include]]
== What this workshop does not include

You walk *one* Java (Maven) supply chain on this OpenShift environment. The items below are out of scope on purpose. Do not look for them in the console or the nav.

[cols="1,2",options="header"]
|===
|Not in this workshop
|What you do instead

|Hosted Konflux
|Builds run as *OpenShift Pipelines* (the Task and BuildConfig names on the cluster). Names you may hear in a customer conversation are in xref:appendix-konflux-mapping.adoc[Konflux mapping]. Do not install Konflux.

|A physical air-gap or data diode
|This environment stays on the network. xref:tssc-14-disconnected-verify.adoc[5.3] is the key-based sign and verify *procedure*. xref:appendix-disconnected-playbook.adoc[Disconnected transfer] is a simulated file drop, not USB or a diode.

|Building Project Hummingbird images
|You *verify and consume* published Red Hat Hardened Images. You do not rebuild the Fedora factory that produces them.

|FIPS certification in the lab
|FIPS is a *product claim* on the image you consume. This workshop does not rebuild an image to produce FIPS evidence.

|JFrog Artifactory, or a Python (FastAPI) application
|The lab application is Java. *Nexus* is the enterprise proxy you configure. Short callouts cover `pip` and Artifactory patterns; you do not install Artifactory or scaffold a second app.

|A live Clearinghouse Premier membership, member-specific GAV, or embargo window
|You practice the *post-disclosure* Network pin and the TSSC controls Premier would reuse (private proxy, exact pin, SoR). You do not request a member-only package or sit inside a real embargo on this claim. Concept: xref:appendix-lightwell-concepts.adoc[Lightwell concepts].

|A customer proof-of-value or certification exam
|This is a *guided workshop*. You practice the flow here. xref:appendix-enablement-checklist.adoc[Internal enablement] is scored *after* the event. It is not a live PoV on this environment.

|A production virtual machine
|You promote a *signed container digest* through GitOps (xref:tssc-15-gitops-admission.adoc[6.1], xref:tssc-16-prod-gitops.adoc[6.2]). The same digest can later run on bootc or Satellite; there is no VM to boot here.
|===

== Honor system

The nav does not lock. You can open 7.2 on day one. Nothing in this workshop pastes the answer for you. If you skip a *Check*, later steps fail for real (wrong `FROM`, no signature, unsigned image denied).

Each gated module has an in-cluster *Validate Job* in `lw-poc-validate`. Do the *Your change*. Fill that module’s report ConfigMap token. Run the Job (delete it if it already exists, create it from ConfigMap `validate-job-templates`, read the logs). Then continue. There is no Solve. Unlimited retries.

Here is 1.1 as an example:

:report-name: report-01-hummingbird-verify
:report-key: consume_published
:report-token: consume-published
include::partial$validate-job-report.adoc[]

:validate-job: validate-01-hummingbird-verify
:validate-job-template: job-01.yaml
include::partial$validate-job-rerun.adoc[]

The same three commands appear on every gated *Check*, with that module’s Job name.

== Before you start

* This guide runs in the RHDP *Showroom* (lab nav plus embedded terminal).
* `oc`, `cosign`, `oc-mirror`, and `ec` are already on `PATH`. Do *not* curl `github.com` for CLIs.
* Console URLs and passwords for Gitea, Keycloak, RHTPA, and Developer Hub are on xref:index.adoc[Welcome]. CLI steps still discover hosts from `demo-userinfo-*` ConfigMaps — do not invent Route hosts. Do not use GitHub as lab git. Gitea is in-cluster.
* Gated *Checks* are Validate Jobs in `lw-poc-validate`. The intro shows how to run one.
* Never commit credentials (`LW_USERNAME` / `LW_PASSWORD`, `student_password`, `cosign.key`).

== Official documentation

Use product docs when a customer asks “where is that written?”, not as a substitute for the Checks.

* link:{lightwell-product-url}[Lightwell^] (Network and Clearinghouse Premier models)
* link:{docs-lwn-lifecycle-url}[Lightwell Network: patch delivery lifecycle^] (ends at public Disclosure / OSV / VEX)
* link:{docs-lwn-choose-repo-url}[Lightwell Network: choose Validated or Remediated^]
* link:{docs-lwn-java-url}[Configure your Java build tool for Lightwell Network^]
* link:{docs-hardened-images-url}[Verify Red Hat Hardened Images^]
* link:{docs-oc-mirror-url}[oc-mirror plugin v2^]
* link:{docs-rhtap-tech-url}[Technologies behind Trusted Application Pipeline^] (RHTAS, RHTPA, RHDH)
* link:{docs-rhtas-url}[Trusted Artifact Signer deployment guide^] (includes Conforma / `ec`)
* link:{docs-rhtpa-url}[Trusted Profile Analyzer overview^]
* link:{docs-rhacs-url}[Advanced Cluster Security: evaluate security risks^]
* link:{docs-gitops-url}[OpenShift GitOps^]
* link:{docs-pipelines-url}[OpenShift Pipelines^]
* link:{docs-rhdh-url}[Red Hat Developer Hub^]

== Start here

Open xref:tssc-01-hummingbird-verify.adoc[1.1 Verify Hummingbird] when you are ready to work.

Consultant maps: xref:appendix-consultant-delivery.adoc[delivery map], xref:appendix-konflux-mapping.adoc[Konflux mapping], xref:appendix-disconnected-playbook.adoc[disconnected transfer]. Instructors: xref:appendix-instructor-runbook.adoc[two-day grouping] (envelope, not a timer). After the event: xref:appendix-enablement-checklist.adoc[internal enablement checklist]. Concepts: xref:appendix-lightwell-concepts.adoc[Lightwell concepts], xref:appendix-acronyms.adoc[acronym glossary].
