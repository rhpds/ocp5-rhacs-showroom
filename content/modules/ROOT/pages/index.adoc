= {lab_name}

include::partial$_attributes.adoc[]

This course offers hands-on experience with Kubernetes-native security on Red Hat^(R)^ OpenShift Container Platform—covering platform foundations, Advanced Cluster Security (RHACS), and Trusted Software Supply Chain (TSSC). Each module ends with a practical *task* so you apply what you learned before moving on.

== How this Roadshow works

* *This page is home* — Credentials and console links live here. Use the home button anytime you get lost.
* *Navigate with the table of contents* — Modules are in the left table of contents, or use *Next* at the bottom of each page.
* *Use consoles in Showroom, or open them in a new tab* — Keep this guide open. Prefer the console tabs on the right of the lab UI. To open a link in a new browser tab: on a Mac, *<cmd> click*; on Windows/Linux, *<ctrl> click* or right-click → "Open in new tab."
* *Use the bastion* — Run `oc`, `roxctl`, and `podman` on the RHEL bastion.
* *Cluster services come from GitOps* — RHACS, Quay, GitOps, Compliance Operator, and the vulnerable demo apps are already synced by Argo CD.
* *Run the bastion setup script* — That step only configures your CLI token and workshop images in Quay.
* *Work the catalog in order, or jump in* — 101 → 201 → 301, then the product deep-dives (RHACS, Virtualization, Lightwell TSSC).

== Environment setup

Cluster-wide install (RHACS, Quay, GitOps, Compliance Operator, demo apps) is already applied by OpenShift GitOps. From the *Terminal* tab, run the remaining bastion steps: RHACS CLI access and workshop images.

[source,bash,subs="+macros,+attributes",role=execute]
----
git clone https://github.com/rhpds/ocp5-rhacs-showroom.git
cd ocp5-rhacs-showroom
bash setup/lab-environment.sh \
  --quay-user {quay_admin_username} \
  --quay-password '{quay_admin_password}'
source ~/.bashrc
----

TIP: Feel free to sign into the consoles while the lab environment script runs in the background.

== All lab components

Before you start a path, confirm you can open each Showroom tab (right side of the lab UI) and sign in. Use these credentials anytime you return to this homepage.

TIP: Using the Showroom allows you to move through the console tabs while you work through the labs.

=== Red Hat^(R)^ OpenShift Container Platform (OCP)

. Open the *OpenShift Console* tab (or go to `{openshift_cluster_console_url}`).
. Sign in with the credentials below.

[cols="1,1"]
|===
|Console URL| {openshift_cluster_console_url}
|API URL| {openshift_api_server_url}
|Username| {openshift_cluster_admin_username}
|Password| {openshift_cluster_admin_password}
|===

=== Red Hat^(R)^ Advanced Cluster Security (RHACS)

. Open the *Red Hat Advanced Cluster Security* tab (or go to `{acs_route}[{acs_route}^]`).
. Sign in with the credentials below.

[cols="1,1"]
|===
|Console URL| {acs_route}[{acs_route}^]
|Username| {acs_portal_username}
|Password| {acs_portal_password}
|===

=== OpenShift GitOps (Argo CD)

. Open the *OpenShift GitOps* tab (or go to `{openshift_gitops_url}[{openshift_gitops_url}^]`).
. Sign in with the credentials below.

[cols="1,1"]
|===
|Console URL| {openshift_gitops_url}[{openshift_gitops_url}^]
|Username| {openshift_gitops_user}
|Password| {openshift_gitops_password}
|===

=== SSO (Keycloak)

TSSC identity for RHTPA. Open the *SSO (Keycloak)* tab (or go to `{keycloak_url}/admin/[{keycloak_url}/admin/^]`). Use `tpa-user` on the RHTPA tab — not this admin console — when the lab asks you to sign in to Trusted Profile Analyzer.

[cols="1,1"]
|===
|Admin console| {keycloak_url}/admin/[{keycloak_url}/admin/^]
|Admin username| {keycloak_admin_username}
|Admin password| {keycloak_admin_password}
|TPA username| {keycloak_username}
|TPA password| {keycloak_password}
|===

=== Red Hat Trusted Profile Analyzer (RHTPA)

. Open the *RHTPA* tab (or go to `{rhtpa_url}[{rhtpa_url}^]`).
. Sign in with the TPA user (Keycloak realm `tpa`).

[cols="1,1"]
|===
|Console URL| {rhtpa_url}[{rhtpa_url}^]
|Username| {rhtpa_username}
|Password| {rhtpa_password}
|===

=== Gitea

. Open the *Gitea* tab (or go to `{gitea_url}[{gitea_url}^]`).
. Sign in with the credentials below.

[cols="1,1"]
|===
|Console URL| {gitea_url}[{gitea_url}^]
|Username| {gitea_username}
|Password| {gitea_password}
|===

=== Red Hat Developer Hub

. Open the *Developer Hub* tab (or go to `{rhdh_url}[{rhdh_url}^]`).
. Choose *Guest* — Developer Hub has no workshop password.

[cols="1,1"]
|===
|Console URL| {rhdh_url}[{rhdh_url}^]
|Sign-in| Guest (no password)
|===

=== Showroom Terminal

Use the *Terminal* tab (or SSH) for CLI work (`oc`, `roxctl`, `podman`, `curl`, etc.).

[cols="1,1"]
|===
|SSH command| `{bastion_ssh_command}`
|Password| {bastion_ssh_password}
|===

== What's in this roadshow

* xref:basic-INDEX.adoc[101 Foundations] — OpenShift secure-by-default posture.
* xref:intermediate-INDEX.adoc[201 Intermediate] — Hardening and shift-left.
* xref:advanced-INDEX.adoc[301 Advanced] — Enterprise and zero trust.
* xref:acs-INDEX.adoc[Advanced Cluster Security (RHACS)] — Setup, vulnerabilities, policy and risk, CI/CD gates, compliance, notifications and API, network and runtime, installation, and xref:acs.adoc[ACS 5 capabilities] (VM scanning, file activity monitoring, MCP, and init containers).
* xref:virt-INDEX.adoc[OpenShift Virtualization Security] — HyperConverged feature gates, virt RBAC, VM and storage isolation, VM networking, and host firmware/kernel controls from the OpenShift Virtualization hardening guide.
* xref:tssc-INDEX.adoc[Lightwell and Red Hat Trusted Software Supply Chain (TSSC)] — Hummingbird, Lightwell remediations, hermetic build, keyless signing, GitOps admission, and TPA/ACS compliance.

